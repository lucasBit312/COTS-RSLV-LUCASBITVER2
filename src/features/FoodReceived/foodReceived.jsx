import React from 'react';
import PropTypes from 'prop-types';
import { Box } from '@mui/material';

foodReceived.propTypes = {
    
};

function foodReceived(props) {
    return (
       <Box marginTop={9}>
            Nhận Thực Phẩm NÈ
       </Box>
    );
}

export default foodReceived;